<?xml version="1.0" encoding="utf-8"?>
<!--
  MSIX manifest for the Microsoft Store build of WinTrash.
  Placeholders to replace before packaging (build-msix.ps1 does this):
    __IDENTITY_NAME__      e.g. 12345YourPublisher.WinTrashToolkit (from Partner Center)
    __PUBLISHER__          e.g. CN=XXXXXXXX-XXXX-... (from Partner Center > Product identity)
    __PUBLISHER_DISPLAY__  your publisher display name
    __VERSION__            4-part version, e.g. 1.4.0.0
-->
<Package
  xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
  xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
  xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities"
  IgnorableNamespaces="uap rescap">

  <Identity Name="__IDENTITY_NAME__"
            Publisher="__PUBLISHER__"
            Version="__VERSION__"
            ProcessorArchitecture="neutral" />

  <Properties>
    <DisplayName>WinTrash Toolkit</DisplayName>
    <PublisherDisplayName>__PUBLISHER_DISPLAY__</PublisherDisplayName>
    <Logo>Assets\StoreLogo.png</Logo>
    <Description>Scans 18 kinds of Windows application leftovers and cleans them selectively - every deletion is confirmed by you and backed up first.</Description>
  </Properties>

  <Dependencies>
    <TargetDeviceFamily Name="Windows.Desktop" MinVersion="10.0.17763.0" MaxVersionTested="10.0.26100.0" />
  </Dependencies>

  <Resources>
    <Resource Language="en-us" />
    <Resource Language="vi" />
    <Resource Language="zh-cn" />
    <Resource Language="ru" />
  </Resources>

  <Applications>
    <Application Id="WinTrash" Executable="WinTrashLauncher.exe" EntryPoint="Windows.FullTrustApplication">
      <uap:VisualElements
        DisplayName="WinTrash Toolkit"
        Description="Windows leftovers scanner and selective cleaner with backups"
        BackgroundColor="transparent"
        Square150x150Logo="Assets\Square150x150Logo.png"
        Square44x44Logo="Assets\Square44x44Logo.png" />
    </Application>
  </Applications>

  <Capabilities>
    <!-- Full trust is required: the app reads/writes the registry, services,
         scheduled tasks and firewall rules outside the AppContainer sandbox. -->
    <rescap:Capability Name="runFullTrust" />
  </Capabilities>
</Package>
